<Page x:Class="SmallFactoryWPF.Pages.MachinesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SmallFactoryWPF.Pages"
      mc:Ignorable="d"
      Loaded="Page_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Name="BTurnFactory" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="20" Margin="0 0 20 0" Click="BTurnFactory_Click" />
        <ScrollViewer Grid.Row="1">
            <StackPanel Name="SPMachines" />
        </ScrollViewer>
    </Grid>
</Page>
